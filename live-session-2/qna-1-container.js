class container {
    constructor() {
        this.pool = new Pool();
    
        const engine = new Engine();
        const engine = new Engine();
        const engine = new Engine();
        const engine = new Engine();
        this.car = new Car(engine, engine, engine, engine);
    }
};

module.exports = container;
